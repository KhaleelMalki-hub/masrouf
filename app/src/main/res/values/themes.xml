<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this exists only so the manifest has a theme
         and the window background matches before the first frame. -->
    <style name="Theme.Masrouf" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
