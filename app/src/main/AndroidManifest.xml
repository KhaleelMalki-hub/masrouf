<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Two of the accounts this app covers have no Android app on this device, so
      their transactions arrive only as SMS. The permission is granted at runtime
      and the app degrades to notification capture alone without it.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!--
      Reads the existing inbox once, to recover the history that predates the app.
      Separate from RECEIVE_SMS on purpose: that one only sees messages arriving
      from now on, and the two banks with no app have years of transactions that
      exist nowhere else. Nothing is stored unless a bank parser recognises it.
    -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!--
      Backup is ON, and the allow-list beside it is what makes that acceptable.

      This app held the opposite line until 2026-09-10: nothing left the device,
      and a new phone therefore started empty. What that costs is not the
      messages - those transfer with the SIM and are read again - but the 185
      categories the owner filed by hand and the 35 merchant rules he taught the
      app. Neither exists in any bank message. They are months of decisions and
      nothing can recreate them.

      `backup_content.xml` (API 26-30) and `data_extraction_rules.xml`
      (Android 12+) name the three files that may be copied and nothing else, so
      a future table does not join the backup by default. The cloud half refuses
      to run on a device that cannot encrypt it; on Android 9 and above the key
      is the phone's own lock secret, so the copy in his Google account is one
      Google cannot read. `MasroufBackupAgent` checkpoints the write-ahead log
      first, so what is copied is one consistent file.
    -->
    <application
        android:enableOnBackInvokedCallback="true"
        android:name=".MasroufApp"
        android:allowBackup="true"
        android:backupAgent=".data.MasroufBackupAgent"
        android:fullBackupContent="@xml/backup_content"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Masrouf">

        <!--
             Frame timing and traces without making the build debuggable. A release
             build is the one worth measuring - it is what the owner runs - and
             `dumpsys gfxinfo` reports nothing for a build that is neither.
        -->
        <profileable android:shell="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Receives bank SMS. BROADCAST_SMS is a signature|privileged permission no
          third-party app can hold, so only the system can deliver to this receiver.
        -->
        <receiver
            android:name=".capture.SmsCaptureReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!--
          Reads bank notifications off the status bar. The permission is held by
          the system and granted by the user in settings, so the app cannot start
          capturing without them having gone there first.
        -->
        <service
            android:name=".capture.MasroufNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
